npm run dev
# sudo npm run dev

# Alternatively, you can start the server without sudo if you are in the docker group
# sudo usermod -aG docker REPLACE_WITH_YOUR_USERNAME
# sudo systemctl restart docker